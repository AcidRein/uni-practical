#include <stdio.h>
#include <stdlib.h>
int main(void) {
    // malloc(size_t size) - returns a pointer to free memory
    // free(void *ptr) - free a memory given pointer

    // Malloc
    int lengthOfArray = 5;
    double myArr[lengthOfArray] = {13.32, 1.333332, 3.3, 1.23, 2};

}