#include <stdio.h>
#include <math.h>

// Compile with gcc {FILE_NAME1}.c -o {FILE_NAME2}
// Run code with ./{FILE_NAME2}

int main(void) {

    /* Hypotenuse 
    double a, b;
    printf("Enter your a value\n");
    scanf("%lf", &a);
    printf("Enter your b value\n");
    scanf("%lf", &b);

    double c1 =  (a * a) + (b * b);
    double c2 = sqrt(c1);

    printf("Your a: %.2f, Your b: %.2f, Your hypotenuse (c): %.2f", a, b, c2);
    return 0;
    */
}